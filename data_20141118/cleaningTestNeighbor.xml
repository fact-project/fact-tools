<module>
 <fact.cleaning.TwoLevelTimeNeighbor
    photonChargeKey="${pc_key}"
    outputKey="${cleaning_key}"
    arrivalTimeKey="${at_key}"
    starPositionKeys="Cetatauri"
    starRadiusInCamera="11.0"
    corePixelThreshold="${core}"
    neighborPixelThreshold="${neighbor}"
    timeThreshold="{time}"
    minNumberOfPixel="2"
/>
<fact.features.NumberOfPixelInShower
    showerKey="${cleaning_key}"
    outputKey="N_${cleaning_key}"
/>
<fact.features.Size
    showerKey="${cleaning_key}"
    photonChargeKey="${pc_key}"
    outputKey="S_${cleaning_key}"
/>
<fact.features.NumberOfIslands
    key="${cleaning_key}"
    outputKey="nI_${cleaning_key}"
/>
<fact.statistics.ArrayStatistics
    key="${pc_key}"
    outputKey="pC_${cleaning_key}"
    pixelSetKey="${cleaning_key}"
/>
<fact.statistics.ArrayStatistics
    key="${at_key}"
    outputKey="aT_${cleaning_key}"
    pixelSetKey="${cleaning_key}"
/>
</module>