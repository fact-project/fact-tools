/**
* <PRE format="md">
* The processors in this package are called filters cause they take the raw fact data as input and put an array of the same length. 
* They usually iterate over the array and smooth it or try to remove artifacts and similar things. These processors usually extend the 
* __SimpleFactEventProcessor<float[], float[]>__ class for more readable code.
* </PRE>
*/
package fact.filter;
